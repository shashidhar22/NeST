import re
import os
import sys
import csv
import vcf
from collections import namedtuple
from collections import OrderedDict
from operator import attrgetter

class Writer:
    
    def __init__(self):
        return

    def writeFasta(self, fasta_obj, out_path):
        out_handle = open(out_path, 'w')
        for lines in fasta_obj:=`=jedi=0, =`=      (self, *_**args*_*, **kwargs) =`=jedi=`=
            header = '{0}_{1}'.format(lines.header, lines.fastaid)
        

